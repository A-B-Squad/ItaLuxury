export const formatDate = (timestamp: string) => {
    return new Date(Number.parseInt(timestamp)).toLocaleDateString('fr-FR', {
      year: 'numeric',
      month: 'short',
      day: 'numeric',
      hour: '2-digit',
      minute: '2-digit'
    });
  };
